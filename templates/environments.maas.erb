    # https://juju.ubuntu.com/docs/config-maas.html
    maas:
        type: <%=@environment_type%>

        # maas-server specifies the location of the MAAS server. It must
        # specify the base path.
        #
        maas-server: '<%= @maas_server%>'

        # maas-oauth holds the OAuth credentials from MAAS.
        #
        maas-oauth: '<%= @maas_oath%>'

        # maas-server bootstrap ssh connection options
        #

        # bootstrap-timeout time to wait contacting a state server, in seconds.
        bootstrap-timeout: <%= @bootstrap_timeout %>

<% @enable_os_refresh_update -%>
        # Whether or not to refresh the list of available updates for an
        # OS. The default option of true is recommended for use in
        # production systems, but disabling this can speed up local
        # deployments for development or testing.
        #
        enable-os-refresh-update: <%= @enable_os_refresh_update %>
<% end -%>
<% @enable_os_upgrade %>
        # Whether or not to perform OS upgrades when machines are
        # provisioned. The default option of true is recommended for use
        # in production systems, but disabling this can speed up local
        # deployments for development or testing.
        #
        enable-os-upgrade: <%= @enable_os_upgrade %>
<% end -%>
